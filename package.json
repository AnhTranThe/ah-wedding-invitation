{
  "type": "module",
  "scripts": {
    "dev": "node generate-firebase-config.js && npx live-server --port=5501 --no-browser"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier": "^4.0.0",
    "terser": "^5.48.0"
  }
}
